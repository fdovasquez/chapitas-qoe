{
  "name": "chapitas-desktop",
  "version": "1.0.0",
  "description": "Aplicacion de escritorio para crear hojas imprimibles de chapitas.",
  "author": "QOE",
  "main": "src/main.js",
  "private": true,
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --publish never",
    "build:win": "electron-builder --win --publish never",
    "build:mac": "electron-builder --mac zip --x64 --arm64 --publish never",
    "build:mac:x64": "electron-builder --mac zip --x64 --publish never",
    "build:mac:arm64": "electron-builder --mac zip --arm64 --publish never",
    "dist": "electron-builder --win --mac --publish never"
  },
  "build": {
    "appId": "cl.qoe.chapitas",
    "productName": "Chapitas QOE",
    "files": [
      "src/**/*",
      "package.json"
    ],
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": [
        "zip"
      ]
    },
    "mac": {
      "target": [
        "zip"
      ],
      "category": "public.app-category.graphics-design"
    },
    "compression": "normal"
  },
  "devDependencies": {
    "electron": "^38.0.0",
    "electron-builder": "^26.0.12"
  }
}
